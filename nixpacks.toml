[phases.setup]
nixPkgs = ["php82", "php82Extensions.mongodb"]

[phases.install]
cmds = ["composer install --optimize-autoloader --no-scripts"]

[phases.build]
cmds = []

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"